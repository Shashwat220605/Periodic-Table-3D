# Accessibility checklist

When changing the interactive periodic-table UI:

- Keep controls visually distinct and consistently positioned.
- Preserve readable labels and sufficient contrast.
- Make interactive elements reachable with the available keyboard controls where supported.
- Avoid relying on animation alone to communicate state.
- Check that opening one interactive panel does not hide the controls needed to close or change it.