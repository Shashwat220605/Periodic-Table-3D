# Testing matrix

For UI changes, check the default desktop viewport and at least one narrower viewport. Verify page load, opening and closing interactive panels, and transitions between related controls. Capture a screenshot for visible layout regressions.