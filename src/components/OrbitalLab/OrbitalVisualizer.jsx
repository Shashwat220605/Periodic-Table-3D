import { useMemo, useState } from "react";
import { Canvas } from "@react-three/fiber";
import { OrbitControls } from "@react-three/drei";
import "./OrbitalVisualizer.css";

const ORBITALS={"1s":{nodes:0,shape:"s",electrons:2,description:"Spherical orbital with electron probability centered around the nucleus."},"2s":{nodes:1,shape:"s",electrons:2,description:"Larger spherical orbital with one radial node."},"2p":{nodes:0,shape:"p",electrons:6,description:"Dumbbell-shaped p orbital. Real atoms have three orientations: px, py and pz."},"3d":{nodes:0,shape:"d",electrons:10,description:"Five d-orbital orientations with more complex angular structure."}};
function Cloud({orbital}){const points=useMemo(()=>{const a=[];for(let i=0;i<4200;i++){const u=Math.random()*2-1,phi=Math.random()*Math.PI*2,r=Math.pow(Math.random(),.32)*1.8,s=Math.sqrt(1-u*u);let x=r*s*Math.cos(phi),y=r*u,z=r*s*Math.sin(phi);if(orbital.shape==="p"){const l=Math.random()>.5?1:-1;y=Math.abs(y)*l*1.35;x*=.62;z*=.62}if(orbital.shape==="d"){const q=Math.sin(phi*2);x*=1.05*q;z*=1.05*q;y*=.85}if(orbital.nodes===1&&Math.abs(r-.75)<.2)continue;a.push(x,y,z)}return new Float32Array(a)},[orbital]);return <points><bufferGeometry><bufferAttribute attach="attributes-position" count={points.length/3} array={points} itemSize={3}/></bufferGeometry><pointsMaterial color="#38bdf8" size={.035} transparent opacity={.3} depthWrite={false}/></points>}
export default function OrbitalVisualizer(){const[open,setOpen]=useState(false),[key,setKey]=useState("2p"),o=ORBITALS[key];return <><button className="orbital-launcher" onClick={()=>setOpen(true)}><span>◌</span><div><small>QUANTUM MODEL</small><b>ORBITAL CLOUDS</b></div></button>{open&&<div className="orbital-overlay"><div className="orbital-panel"><div className="orbital-header"><div><small>PROBABILITY DISTRIBUTION</small><h2>Real Orbital Visualization</h2><p>Electron orbitals are shown as probability clouds, not planetary paths.</p></div><button onClick={()=>setOpen(false)}>×</button></div><div className="orbital-layout"><div className="orbital-canvas"><Canvas camera={{position:[0,0,5.2],fov:42}}><ambientLight intensity={1.1}/><Cloud orbital={o}/><mesh><sphereGeometry args={[.12,32,32]}/><meshBasicMaterial color="#fff"/></mesh><OrbitControls enablePan={false}/></Canvas></div><div className="orbital-info"><div className="orbital-label">SELECT ORBITAL</div><div className="orbital-tabs">{Object.keys(ORBITALS).map(k=><button className={k===key?"selected":""} key={k} onClick={()=>setKey(k)}>{k}</button>)}</div><h3>{key}</h3><p>{o.description}</p><div className="orbital-stat"><span>MAX ELECTRONS</span><b>{o.electrons}</b></div><div className="orbital-stat"><span>RADIAL NODES</span><b>{o.nodes}</b></div><div className="orbital-note">The cloud represents regions where an electron is more likely to be detected. This is a probability-density visualization.</div></div></div></div></div>}</>}
